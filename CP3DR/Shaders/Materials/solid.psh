
sampler2D diffuseSampler : register(s0);

float4 main(float4 TexCoord : TEXCOORD0) : COLOR0
{
	//float4 diffuseColor = tex2D(diffuseSampler, TexCoord.xy);

	//return diffuseColor;
	return float4(1.0, 1.0, 1.0, 1.0);
}
